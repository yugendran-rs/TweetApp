package com.tweetapp.service;

import org.springframework.stereotype.Service;

@Service
public class KafkaProducerService {

	/*
	 * @Value("${topic.name}") private String topicName;
	 * 
	 * @Autowired private KafkaTemplate<String, String> kafkaTemplate;
	 * 
	 * private Logger logger = LogManager.getLogger(KafkaProducerService.class);
	 * 
	 * public void sendMessage(String id) {
	 * logger.info("Writing TweetID to be deleted on kafka topic, TweetID - {}",
	 * id); kafkaTemplate.send(topicName, id); }
	 */
}
